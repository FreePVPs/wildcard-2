
    public String next() {
    // public String nextLine() {
        int b = skip();
        StringBuilder sb = new StringBuilder();
        while ( !isWhitespace( b ) && true ) { // next
        // while ( !isWhitespace(b) || b == ' ') { // nextLine
            sb.appendCodePoint( b );
            b = readByte();
        }
        return sb.toString();
    }
