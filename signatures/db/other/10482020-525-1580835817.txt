    long long int i,j,k=0,l,n,temp,c[100005];
    i=s;
    j=m+1;
    while((i<=m)&&(j<=e))
    {
        if(a[j]<a[i])
        {
            c[k++]=a[j++];
        }
        else
        {
            c[k++]=a[i++];
        }


    }
    while(i<=m)
    {
        c[k++]=a[i++];
    }
    while(j<=e)
    {
        c[k++]=a[j++];
    }

    i=s;
    k=0;
    while(i<=e)
    {
     a[i++]=c[k++];
    }